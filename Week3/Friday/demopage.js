function promptfunciton(){
var prompt = prompt("Set the height and width of the canvas", row, colums);
  if prompt === sting

}
for (i = 0, i < rows, i++){
  var newRow = document.createElement("row");
    for (j=0, j < colums, i++){
        var newColumn = document.createElement("colum");
    }
}
